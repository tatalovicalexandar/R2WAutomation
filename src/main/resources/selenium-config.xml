<?xml version="1.0" encoding="UTF-8"?>
<config>
    <!-- Supported browserName values: Chrome, Firefox, Edge -->
    <browserName>Firefox</browserName>

    <!-- Supported environment name: r2w replica, r2w mirror, karcher,... -->
    <environmentName>R2W Replica Environment</environmentName>

    <!-- Supported environmentURL values -->
    <environmentURL>https://kaercher-replica.ready2wash.net</environmentURL>

    <!-- Timeout period in seconds -->
    <timeout>10</timeout>

    <takeScreenshots>true</takeScreenshots>

    <reportName>Ready2Wash Automation Smoke Results</reportName>
</config>